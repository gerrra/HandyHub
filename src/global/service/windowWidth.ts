export const maxWidth1240: boolean = window.innerWidth <= 1240;

export const maxWidth900: boolean = window.innerWidth <= 900;

export const maxWidth640: boolean = window.innerWidth <= 640;

export const maxWidth400: boolean = window.innerWidth <= 400;